"""JSON report renderer.

Serializes findings (including their stable fingerprint) into deterministic JSON
suitable for tooling, the CI differ, and as the canonical pipeline artifact.

Implemented in a later phase.
"""
