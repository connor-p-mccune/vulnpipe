"""Prioritization / ordering of findings.

Orders findings deterministically by severity, then CVSS, then EPSS, so the most
actionable issues surface first in reports. Pure function.

Implemented in a later phase.
"""
