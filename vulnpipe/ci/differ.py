"""Baseline vs current diff.

Compares current findings against a baseline by fingerprint and classifies each as
new / persisting / resolved. Deterministic for fixed input.

Implemented in a later phase.
"""
